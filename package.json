{
  "name": "atomic-ui",
  "version": "0.0.1",
  "description": "UI library of atomic components and primitives",
  "main": "index.js",
  "repository": "git@github.com:mchadwickweb/atomic-ui.git",
  "author": "Michael Chadwick <michael@interactstudio.co.uk>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/app dev",
    "build:app": "yarn --cwd packages/app build",
    "lint:app": "yarn --cwd packages/app eslint --ext .jsx,.js src/",
    "lint:elements": "yarn --cwd packages/elements eslint --ext .jsx,.js src/",
    "lint": "npm-run-all -l -p lint:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
