{
  "name": "@atomic-ui/core",
  "description": "Atomic UI - Core",
  "version": "0.0.1",
  "scripts": {
    "clean": "rimraf lib",
    "build:types": "tsc --emitDeclarationOnly --outDir ./lib",
    "build:core": "node ./esbuild.js",
    "build": "npm-run-all clean build:types build:core"
  },
  "devDependencies": {
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "esbuild": "^0.14.49",
    "npm-run-all": "^4.1.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
